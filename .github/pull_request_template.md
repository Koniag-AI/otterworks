## Summary

<!-- What changed and why. -->

## ServiceNow incident

INC<!-- number, e.g. INC0010042. Leave the line out entirely for changes with no ticket. -->

<!--
The Harness pipeline resolves the incident to update from the first INC<digits>
token in the PR title (and, on merge-triggered runs, in the merge commit
message), so keep the token in the title as well:
    fix(file-service): reject uploads over the configured limit [INC0010042]
-->

## Verification

<!-- Lint/tests run, and how the reported behavior was confirmed fixed. -->

## Rollback

<!-- How to undo this if the deploy misbehaves (revert, flag, config change). -->
